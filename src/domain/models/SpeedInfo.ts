export interface SpeedInfo {
  downloadSpeed: number; // Bytes per second
  uploadSpeed: number;   // Bytes per second
  downloadUnit: string;  // e.g., B/s, KB/s, MB/s
  uploadUnit: string;    // e.g., B/s, KB/s, MB/s
}
